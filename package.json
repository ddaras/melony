{
  "name": "melony-monorepo",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "pub": "turbo run pub",
    "version:patch": "npm version patch",
    "push:landing": "git subtree push --prefix=apps/landing landing-remote main"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
