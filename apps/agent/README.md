# Agent Server

A basic Hono server for the Melony agent.

## Development

```bash
pnpm dev
```

The server will start on `http://localhost:3000` (or the port specified by the `PORT` environment variable).

## Build

```bash
pnpm build
```

## Start

```bash
pnpm start
```
